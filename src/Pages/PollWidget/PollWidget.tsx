import {mockPolls} from './mockPollData'

const PollWidget = () => {
  return (
    <div>
      <h1 style={{color:'gray'}}>Poll Widget</h1>
      <Polling />
    </div>
  );
};

export default PollWidget;





const Polling=()=>{
  return (
    <div>
    </div>
  );

}