import { mockPolls } from './mockPollData';

const PollWidget = () => {
  return (
    <div>
      <h1 style={{color:'plum'}}>Poll Widget</h1>
      <Polling />
    </div>
  );
};

export default PollWidget;





const Polling=()=>{
  return (
    <div>
    </div>
  );

}